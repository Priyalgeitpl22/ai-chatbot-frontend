export enum ThreadType {
  UNASSIGNED = "unassigned",
  ASSIGNED = "assigned",
  OPEN = "open",
  CHAT = "chat",
  BOTS = "bots",
}

export enum Priority {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low'
}


export enum DropDownPurpose {
  Task="task",
  Thread="thread"
}